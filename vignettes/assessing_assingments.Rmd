---
title: "2. Assessing assingments"
---

**Created:** `r Sys.time()`

```{r setup, include = F}
library(knitr)
knitr::opts_chunk$set(list(echo=TRUE,
                           eval=TRUE,
                           cache=TRUE,
                           warning=FALSE,
                           message=FALSE))
opts_chunk$set(fig.width = 10, fig.height = 10)
library(stringr)
library(tidyverse)
options("download.file.method"="curl")
```
